{
  "name": "@gcr/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain. ReviewRun aggregate, state machine, port interfaces, formatter, policy. Zero I/O.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./ports": "./dist/ports/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
