{
  "name": "@gcr/queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SQLite-backed durable job queue. ~300 LOC of polling, locking, retry, backoff.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@gcr/core": "workspace:*",
    "@gcr/observability": "workspace:*",
    "@gcr/storage": "workspace:*",
    "kysely": "^0.27.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
